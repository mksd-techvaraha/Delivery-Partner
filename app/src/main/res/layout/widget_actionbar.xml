<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorPrimary"
    android:layout_height="?android:attr/actionBarSize">

    <ImageView
        android:id="@+id/xIvUpButton"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_back_white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/eight"
        android:layout_marginLeft="@dimen/eight"
        android:layout_marginTop="@dimen/fifteen"
        android:layout_marginBottom="@dimen/eight"
        android:contentDescription="@string/back_button"/>

    <TextView
        android:id="@+id/xTvActionTitle"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        style="@style/textViewWhiteHead"
        android:textSize="@dimen/action_bar_title_text_size"
        tools:text="Order #1234"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@id/xIvUpButton"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="@dimen/eight"
        android:layout_marginLeft="@dimen/eight"
        android:layout_marginTop="@dimen/eight"/>

    <TextView
        android:id="@+id/xTvActionSubTitle"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        style="@style/textViewWhiteHead"
        android:textSize="@dimen/action_bar_subtitle_text_size"
        tools:text="Delivery before"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@id/xIvUpButton"
        app:layout_constraintTop_toBottomOf="@id/xTvActionTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/eight"
        android:layout_marginLeft="@dimen/eight"/>

</androidx.constraintlayout.widget.ConstraintLayout>